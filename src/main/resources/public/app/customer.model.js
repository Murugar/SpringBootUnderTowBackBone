/* Model and Collection Global objects. This is done for conveinence for really
 * should eliminate globals and using an AMD module laading framework like RequireJS
 */

Customer = Backbone.Model.extend({
	
});

Customer.Collection = Backbone.Collection.extend({
// url:'api/project/all', // URL IF server is available

});

